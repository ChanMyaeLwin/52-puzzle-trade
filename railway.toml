[build]
builder = "NIXPACKS"
buildCommand = "cd server && npm install && npm rebuild better-sqlite3"

[deploy]
startCommand = "cd server && node index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
